<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.basakcoding.basak.mapper.CommentMapper">
	<select id="adminSelect" resultType="commentDTO">
		SELECT comment_id, question_id, title, admin_id, name, content, created_at 
		FROM comments c 
		JOIN question q ON c.queston_id=q.question_id 
		JOIN admins a ON c.admin_id=a.admin_id
	</select>
	
</mapper>